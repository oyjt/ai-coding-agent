{
  "name": "@ai-coding-agent/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "aca": "dist/index.js" },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm build && node --test dist/**/*.test.js",
    "lint": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@ai-coding-agent/config": "workspace:*",
    "@ai-coding-agent/core": "workspace:*"
  }
}
