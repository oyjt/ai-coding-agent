# Claude 开发指南

本项目的通用 AI 开发规范以 `AGENTS.md` 为准。

开始工作前：

1. 阅读 `AGENTS.md`。
2. 根据当前任务阅读 `docs/` 中相关文档。
3. 阅读相关源码、模板和测试。
4. 遵循任务分级、Spec 和验证要求。

不要在 `CLAUDE.md` 中重复维护项目规范；项目设计发生变化时优先更新 `AGENTS.md` 和对应的 `docs/*.md`。
